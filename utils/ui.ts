/**
 * Shared UI class strings for the warm "Modern Tactile" brand, so the same
 * Tailwind recipes aren't copied across pages.
 */

/** Standard warm form input / select / textarea. */
export const INPUT_CLASS =
    'w-full bg-surface border border-brand-border rounded-xl px-3.5 py-2.5 text-sm text-brand-text placeholder-brand-text-muted focus:ring-2 focus:ring-sp-green/30 focus:border-sp-green outline-none transition-colors';
